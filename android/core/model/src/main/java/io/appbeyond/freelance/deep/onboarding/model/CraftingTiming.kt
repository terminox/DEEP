package io.appbeyond.freelance.deep.onboarding.model

/** Ported from `CraftingSpaceView`'s step timings. */
object CraftingTiming {
  const val STEP_MILLIS = 520L
  const val STEP_MILLIS_REDUCED_MOTION = 250L
  const val SETTLE_MILLIS = 450L
  const val STEP_COUNT = 5
}
